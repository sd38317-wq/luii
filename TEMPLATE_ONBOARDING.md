# 새 고객 온보딩 체크리스트 (예약 문자 관리 프로그램 판매용)

이 문서는 지금 이 앱(luii, 바른프라이빗키즈룸용 예약 SMS 관리 도구)을 **다른 무인카페/예약 운영자에게
판매**할 때, 고객 한 명이 늘어날 때마다 무엇을 해야 하는지 정리한 체크리스트입니다.

**핵심 방식**: 고객마다 완전히 독립된 배포본을 하나씩 만들어줍니다 (한 서버에서 여러 고객이 로그인
하는 SaaS 구조가 아님). 코드는 그대로 두고, 아래 항목만 고객마다 새로 채우면 됩니다.

**계정을 어디까지 나눠야 하는지**: 알리고만 고객마다 따로면 됩니다. Fly(서버 호스팅), Anthropic
(사진 자동입력), Resend(이메일 알림)는 전부 **사장님 계정 하나로 모든 고객 앱을 운영**해도 문제
없습니다 — 이건 그냥 "코드가 어디서 도는지" 인프라일 뿐이라, 발신자 명의가 중요한 문자 발송(알리고)
과는 성격이 다릅니다. 고객 입장에서는 Fly 계정을 만들 필요조차 없고, 알리고 가입 + 발신번호 등록만
하면 됩니다.

> ⚠️ 이 문서는 "무엇을 준비해야 하는지"를 미리 정리해둔 것이고, 아직 실제로 판매용 코드/저장소
> 작업(아래 "0. 최초 1회 준비 작업")은 진행하지 않았습니다. 첫 고객이 실제로 정해지면 그때 진행합니다.

---

## 0. 최초 1회 준비 작업 (첫 고객 받기 전에 한 번만)

지금 이 저장소(`sd38317-wq/luii`)는 사장님의 카페 전용으로 만들어져 있어서, 그대로 남에게 넘기면
안 됩니다. 판매를 시작하기 전에 딱 한 번 해야 하는 일:

1. **별도의 새 GitHub 저장소 생성**: luii와는 별개로, 판매용 "원본 템플릿" 저장소를 하나 만듭니다.
   지금 luii 코드를 복사해서 시작점으로 씁니다.
2. **브랜딩 제거**: 로그인 화면/브라우저 탭 제목 등에 하드코딩되어 있는 "바른프라이빗키즈룸 _
   괴정점"이라는 문구를, `/admin` 설정 화면에서 입력하는 **카페명 설정값을 그대로 표시**하도록
   코드를 바꿉니다 (고객마다 자기 가게 이름이 자동으로 뜨게). 워터마크 로고(`barun-logo-bg.png`)도
   제거하거나 선택적으로 만듭니다.
2-1. **기본 문구(②③번 메시지) 중립화 — 중요**: `src/lib/messageTemplates.ts`의
   `DEFAULT_CHECKOUT_NOTICE_TEMPLATE`/`DEFAULT_FOLLOWUP_TEMPLATE`은 지금 사장님 가게 전용
   내용(세븐일레븐 10% 환급, 배민 상품권 1만원권 등)이 그대로 박혀 있습니다. 고객이 `/admin`에서
   문구를 안 채우면 이 내용이 그대로 나가버리므로, **템플릿 저장소에서는 이 기본값을 "이용해주셔서
   감사합니다, 리뷰 부탁드려요" 같은 진짜 중립적인 문구로 바꿔야 합니다.** 추가로, 온보딩 시
   "고객이 `/admin`에서 문구를 직접 확인/입력했는지" 체크(아래 1번 체크리스트에 반영)까지 같이
   해두면, 기본 문구가 실수로 그대로 나가는 사고를 이중으로 방지할 수 있습니다.
3. **문자 발송 방식 되돌리기**: 지금 luii는 barun과 공유하는 relay(중계 서버)를 거쳐서 문자를
   보내는데, 이건 사장님의 두 개인 앱끼리만 쓰는 전용 구조입니다. 판매용 템플릿은 **각 고객이 자기
   알리고 계정으로 직접 문자를 보내는 방식**(relay 이전의 원래 방식)으로 되돌려야 합니다. 남의
   문자를 사장님의 relay로 보내면 안 됩니다 (아래 "왜 relay를 공유하면 안 되는지" 참고).
4. 위 작업이 끝나면, 이 템플릿 저장소를 고객마다 복사(GitHub의 "Use this template" 기능 등)해서
   쓰면 됩니다.

## 1. 새 고객이 정해지면 (고객 1명당 반복)

- [ ] **고객과 확인할 것**: 가게 이름, 주소, 주차 안내, 이용 수칙, 문자에 넣을 안내 문구, 관리자로
      쓸 비밀번호
- [ ] **고객이 준비해야 할 것 (이것뿐)**: 알리고(smartsms.aligo.in) 계정 가입 + 본인 발신번호
      등록 (본인 명의 사업자 인증 필요할 수 있음, 알리고 정책에 따름). Fly 계정이나 다른 서비스
      가입은 필요 없습니다 — 전부 사장님 계정 안에서 처리합니다.
- [ ] **새 Fly.io 앱 생성 (사장님 계정 안에)**:
  ```bash
  fly launch --no-deploy   # 앱 이름을 고객별로 고유하게 (예: xx카페-예약)
  fly volumes create kidscafe_data --size 1 --region sin
  ```
- [ ] **환경변수(시크릿) 등록** — 알리고 값만 고객 전용, 나머지는 사장님 계정 재사용 가능:
  ```bash
  fly secrets set \
    ADMIN_PASSWORD="고객 전용 비밀번호" \
    SESSION_SECRET="새로 생성한 랜덤 문자열" \
    CAFE_NAME="고객 가게 이름" \
    ALIGO_USER_ID="고객 알리고 아이디" \
    ALIGO_API_KEY="고객 알리고 API 키" \
    ALIGO_SENDER_NUMBER="고객 발신번호" \
    ANTHROPIC_API_KEY="사장님 기존 키 재사용 가능" \
    RESEND_API_KEY="사장님 기존 키 재사용 가능"
  ```
- [ ] **알리고 발신 IP 등록**: 이 새 Fly 앱의 아웃바운드 IP를 고객의 알리고 계정에 등록. 재배포 시
      IP가 바뀔 수 있으므로, 안정적으로 운영하려면 `fly machine egress-ip allocate`로 고정 IP
      할당 권장 (앱당 비용 발생, 아래 "비용 구조" 참고). 고객 수가 늘어나면 이 앱들도 barun/luii
      처럼 relay 하나를 공유하는 구조로 나중에 다시 묶을 수 있습니다 — 다만 그 경우엔 "왜 relay를
      고객끼리 공유하면 안 되는지"에서 설명한 문제(비용/신뢰도/권한 회수)를 relay 쪽에서 고객별로
      분리 처리하도록 relay 자체를 더 발전시켜야 합니다. 지금 단계에서는 고민하지 않아도 됩니다.
- [ ] **배포**: `fly deploy`
- [ ] **테스트**: 테스트 예약을 만들어서 실제 문자가 고객 발신번호로 정상 도착하는지 확인
- [ ] **`/admin` 설정 화면에서 문구 채우기 (오픈 전 필수)**: 카페 이름/주소/주차/이용수칙, ②③번
      문자 문구, 리뷰 링크, 상품권/환급 여부에 따른 080 무료수신거부 번호 (광고성 문구가 들어가면
      법적으로 필요, README의 "광고 표기" 참고). **②③번 문구는 기본값(중립화된 문구)이 이 고객
      가게에 맞는 내용인지 반드시 확인 — 특히 상품권/환급/이벤트처럼 사장님 가게에만 있던 혜택이
      실수로 안 들어갔는지 확인.**
- [ ] **고객에게 전달**: 관리자 페이지 URL + 비밀번호, 간단한 사용법 안내

## 2. 왜 relay(중계 서버)를 고객끼리 공유하면 안 되는지

지금 luii/barun이 relay를 공유하는 건 **둘 다 사장님 소유의 앱이라 한 알리고 계정 비용을 사장님이
전부 책임지는 게 자연스럽기 때문**입니다. 하지만 완전히 남인 고객의 문자를 사장님 relay로 보내면:

- 그 고객이 보낸 모든 문자 비용이 사장님 알리고 계정에서 빠져나갑니다 (별도 정산 시스템이 없으면
  사장님이 대신 내주는 셈)
- 고객이 스팸성/불법 문자를 보내면 사장님 알리고 계정 전체의 신뢰도(발송 가능 여부)에 영향
- 고객이 서비스를 그만둬도 문자 발송 권한을 회수하기가 애매함

그래서 각 고객은 **자기 명의의 알리고 계정 + 자기 발신번호**를 쓰는 것이 원칙입니다.

## 3. 비용 구조 (참고용, 가격 정책은 사장님이 정하기)

고객 1명 추가 시 실제로 발생하는 비용:

| 항목 | 대략적 비용 | 누가 내는지 |
|---|---|---|
| Fly.io 앱 호스팅 | 월 수천원 수준 (512MB 기준) | **사장님 계정에서 일괄 부담**, 고객 이용료에 포함시켜 청구 |
| Fly 고정 egress IP (선택) | 월 ~$3.60 | 위와 동일, 사장님 계정에서 부담 |
| 알리고 문자 발송료 | 건당 수십원 (SMS/LMS에 따라 다름) | 고객 본인 알리고 계정에서 충전/차감 (고객이 직접 관리) — 유일하게 사장님이 안 내는 항목 |
| Anthropic Claude API (사진 자동입력, 선택 기능) | 사진 1장당 매우 저렴 | **사장님 계정 키 그대로 재사용**해서 사장님이 부담 (고객 수가 많이 늘어나면 총합 기준으로 이용료에 반영) |

**이용료를 얼마로 책정할지, 초기 세팅을 무료로 해줄지 유료로 할지는 사업적 판단**이라 여기서
정하지 않았습니다. 나중에 논의해서 정하면 됩니다.

## 4. 코드 외적으로 미리 생각해볼 것 (참고, 지금 당장 안 해도 됨)

- **개인정보 처리**: 예약자 이름/전화번호를 저장하고 문자로 발송하는 서비스라, 고객(카페 사장님들)이
  본인 손님들에게 개인정보 수집·이용 동의를 받고 있는지는 그 고객의 책임이지만, 이용약관에 명시해두면
  좋습니다.
- **간단한 이용약관/책임 범위**: "문자 발송 실패/지연에 대한 책임은 통신사/알리고 사정에 따를 수
  있다" 같은 면책 조항 정도는 있는 게 안전합니다.
- **지원 범위**: 고객이 늘어나면 "설치는 해주지만 이후 운영은 각자 알아서"인지, "계속 유지보수까지
  봐주는지"를 미리 정해두면 나중에 덜 피곤합니다.

---

첫 고객이 정해지면, 이 문서의 "0. 최초 1회 준비 작업"부터 같이 진행하면 됩니다.
